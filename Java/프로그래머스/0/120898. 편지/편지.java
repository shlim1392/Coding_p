public class Solution {
    public int solution(String message) {
        int charWidth = 2;
        int messageLength = message.length();
        int totalWidth = messageLength * charWidth;
        
        return totalWidth;
    }
}
